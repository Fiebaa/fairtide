{
  "name": "fairtide",
  "version": "0.1.0",
  "description": "A fair pricing API — prices rise and fall like the tide, so everyone gets a fair deal",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts",
    "test": "bun test",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "bun run src/db/migrate.ts",
    "db:seed": "bun run src/db/seed.ts"
  },
  "keywords": ["fair-pricing", "api", "hono"],
  "author": "",
  "license": "AGPL-3.0-only",
  "dependencies": {
    "@hono/swagger-ui": "^0.5.3",
    "@hono/zod-openapi": "^1.2.1",
    "drizzle-orm": "^0.45.1",
    "hono": "^4.11.9",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "bun-types": "^1.3.9",
    "drizzle-kit": "^0.31.9"
  }
}
