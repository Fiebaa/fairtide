# Server
PORT=3000

# Database
DB_PATH=./data/fairify.db

# Logging
LOG_LEVEL=info  # debug | info | warn | error

# CORS
CORS_ORIGIN=*  # comma-separated origins, or * for all

# Rate Limiting
RATE_LIMIT_MAX=100        # max requests per window
RATE_LIMIT_WINDOW_MS=60000  # window duration in ms
